import UIKit

enum Resources {
    enum Symbols {
        static let plus = "+"
        static let active = "✅"
    }
    
    enum Identifiers {
        static let toDoTableViewCell = "ToDoTableViewCell"
    }
}
